#!/usr/bin/env python
# -*- coding: utf-8 -*-

"""Legacy setup.py kept for compatibility with older tooling.
Use `pip install .` / `python -m build` with pyproject.toml by default.
"""

from setuptools import setup

setup()
